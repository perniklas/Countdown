# Valheim Biomes Implementation Plan

> **For agentic workers:** Use superpowers:subagent-driven-development. Original request: Luna subagents implement product code. Execution update: after two Luna workers failed to respond, the user explicitly authorized direct implementation by the parent.

**Goal:** Replace Paper Riot with a Valheim atmosphere offering the first five progression biomes, with especially warm and cozy Meadows.

**Architecture:** Keep the existing theme registry, editor and repository structure. Store an optional biome in themeSettings, normalize old Paper Riot records to Valheim on read, and render a lightweight SVG/CSS landscape through ThemeScene. Use one focused Valheim scene component and stylesheet; no new dependencies.

**Tech Stack:** React, TypeScript, Vite, CSS, SVG, Firebase Firestore and Hosting.

**Spec:** User request in this task: Paper Riot replacement; biome select similar in placement to Glasswater mood slider; five distinct biomes; Meadows prioritized; Luna implementation; push and Firebase deployment. User has explicitly approved proceeding.

## Global Constraints

- User approved direct implementation after Luna workers stalled. Work in the current clean checkout with sequential edits.
- Keep code simple and preserve other theme behavior.
- Follow src/app/agents.md: no new tests or testing frameworks. Update obsolete expectations in existing tests only where necessary.
- Biomes: meadows, black-forest, swamp, mountains, plains. Default and malformed/missing biome fallback: meadows.
- Existing paper-riot records must remain visible and editable in both local storage and Firestore.
- Maintain owner-only Firestore access. Deploy compatible rules before Hosting.
- No external assets required. Landscapes use original SVG/CSS, not copied game assets.

## Review Focus

- Old persisted Paper Riot records must not be rejected by repository validators.
- Selected biome survives save, reload and editing without losing gradientMood.
- Invalid or missing biome values render Meadows; legacy documents remain archivable.
- Theme changes do not leak biome styling into other atmospheres.
- Mobile layout, readable countdown text, keyboard selection, and reduced motion remain usable.

## Task 1: Complete Valheim replacement

**Files:** src/domain/countdown.ts, src/domain/theme.ts, src/domain/editor.ts, src/services/localStorageCountdownRepository.ts, src/services/firebaseCountdownRepository.ts, src/components/CountdownEditor.tsx, src/components/ThemeScene.tsx, new src/components/ValheimScene.tsx, src/app/CountdownApp.tsx, src/styles.css, new src/styles/themes/valheim.css, delete src/styles/themes/paper-riot.css, firestore.rules. Update existing test expectations only if invalidated by the replacement.

**Interfaces:** ThemeId uses valheim instead of paper-riot. Export ValheimBiome = 'meadows' | 'black-forest' | 'swamp' | 'mountains' | 'plains'; themeSettings includes biome?: ValheimBiome for old callers. Export VALHEIM_BIOMES metadata and normalizeValheimBiome(value: unknown): ValheimBiome. ThemeScene accepts biome?: ValheimBiome. Root receives data-biome only for Valheim.

- [x] Add biome metadata and normalization; replace registry entry with Valheim. Normalize legacy theme identifiers only at read boundaries; unknown themes retain existing rejection behavior. themeById('paper-riot') resolves Valheim.
- [x] Extend draft conversion/defaults and both repository read/write paths. Preserve the existing shape for non-Valheim inputs when no biome was supplied, to avoid unnecessary schema churn. Never serialize undefined fields to Firestore.
- [x] Add a labeled native select below atmosphere choices, visible for Valheim, with the five names and a short biome description. Selection persists with countdown settings.
- [x] Pass normalized biome through CountdownApp and ThemeScene. Build ValheimScene as decorative aria-hidden SVG/CSS layers, with no event interception. Remove old Paper Riot visuals and stylesheet import; update swatch and library dot styling.
- [x] Build five distinct landscapes: Meadows has warm sun disc and broad diagonal rays, soft sky, layered green hills, oak/birch silhouettes and pollen; Black Forest has dense dark pines, cool blue mist and faint glowing motes; Swamp has bare twisted trunks, murky water, rain and green fog; Mountains has angular snowy ridges, icy blue moonlight and falling snow; Plains has amber grasses, open sky, distant rock pillars and warm dusty haze. Give each its own atmosphere, palette, foreground and motion rather than only hue shifts. Keep the countdown foreground readable and add restrained Nordic typography/panel treatment.
- [x] Scope styles to Valheim. Use fixed deterministic scenery, responsive viewBox geometry, restrained animation counts, and a prefers-reduced-motion rule that disables animations and transforms for these layers.
- [x] Extend Firestore allowlisted themes with valheim while retaining paper-riot for legacy compatibility. Allow optional biome only from the five values while preserving required numeric gradientMood and owner checks.
- [x] Run npm run typecheck, npm run lint, npm run test:run, npm run build. Fix regressions; record exact evidence and known baseline failures. Do not add tests.
- [x] Direct implementation and verification evidence recorded below after user-approved execution change.

## Task 2: Review and release

- [x] Diff package produced and independent reviewer dispatched; reviewer also failed to respond and was interrupted. Parent reviewed the final diff directly.
- [x] Start a local Vite preview and inspect guest-mode create/edit/reload across all five biomes, including mobile and reduced motion if browser capabilities support it.
- [x] Run any outstanding verification after fixes, review git diff, commit the plan and implementation, push main.
- [ ] Deploy firestore:rules and hosting to configured countdown-57acd project. Confirm successful deployment and live Hosting response.

## Progress

- Pulled main with git pull --ff-only: already up to date. Clean starting revision: 16ec0d9c32ae83b3a6d084d735a94d6e6bb9c772.
- Shell sandbox helper cannot initialize; execution requires approved escalation.

- Execution update: two Luna agents produced no output; both were interrupted. User explicitly approved direct implementation.
- Implementation complete. All 36 existing tests pass; production build and typecheck pass. Lint has zero errors and one pre-existing effect-dependency warning.
- Browser checks: all five biome save/reload/edit flows pass, legacy local Paper Riot renders Meadows, reduced motion stops animations, mobile has no horizontal overflow, and no page errors. Visually inspected desktop for all biomes and mobile Meadows; corrected serif clipping and mobile scenery placement.
